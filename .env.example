# Where the programmes come from.
# Unset  -> the bundled TypeScript files in src/lib (default, always works)
# Set    -> that WordPress, via /wp-json/wp/v2/programmes and /omra-programmes
#
# Local CMS (start "capital-cms" in LocalWP first):
# VITE_CMS_URL=http://capital-cms.local
